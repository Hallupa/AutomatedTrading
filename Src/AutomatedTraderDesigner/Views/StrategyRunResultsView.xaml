<Page x:Class="AutomatedTraderDesigner.Views.StrategyRunResultsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:charting="clr-namespace:TraderTools.Core.UI;assembly=TraderTools.Core.UI"
      xmlns:views1="clr-namespace:TraderTools.Core.UI.Views;assembly=TraderTools.Core.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StrategyRunResultsView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1.5*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0">
            <charting:DoubleChartView/>
        </Grid>

        <Label Grid.Row="1">Results</Label>

        <TabControl Grid.Row="2">
            <TabItem Header="Trades">
                <views1:TradeListControl/>
            </TabItem>
            <TabItem Header="Results">
                <views1:TradesResultsControl DataContext="{Binding ResultsViewModel}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Page>